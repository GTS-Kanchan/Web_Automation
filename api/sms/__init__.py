"""api/sms — SMS API clients. Empty scaffold: no CPaaS API endpoints have
been confirmed against the live app yet (the suite is UI-only today). Add a
concrete client here (e.g. sms_client.py's SmsClient(BaseApiClient)) once
real endpoints are confirmed — see api/common/base_client.py."""
